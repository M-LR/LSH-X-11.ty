---
layout: base.njk
title: Page non trouvée - Les Studios du Héron
permalink: 404.html
eleventyExcludeFromCollections: true
---

<div class="error-page">
    <div class="container">
        <div class="error-content">
            <h1 class="error-code">404</h1>
            <h2 class="error-title">Oups ! Cette scène n'est pas au script.</h2>
            <p class="error-text">
                Il semble que la page que vous cherchez ait été coupée au montage ou n'ait jamais existé. 
                Ne restez pas dans le noir, retournez vers la lumière.
            </p>
            <div class="error-actions">
                <a href="{{ '/' | url }}" class="btn">Retour à l'accueil</a>
            </div>
        </div>
    </div>
</div>

<style>
.error-page {
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: var(--space-2xl) 0;
}

.error-code {
    font-size: clamp(6rem, 20vw, 12rem);
    font-weight: 900;
    line-height: 1;
    color: var(--color-accent);
    margin-bottom: var(--space-md);
    font-family: var(--font-display);
    opacity: 0.2;
    position: relative;
    z-index: 0;
}

.error-content {
    position: relative;
    z-index: 1;
    margin-top: -4rem;
}

.error-title {
    font-size: clamp(var(--font-size-2xl), 5vw, var(--font-size-4xl));
    margin-bottom: var(--space-md);
    color: var(--color-text);
}

.error-text {
    font-size: var(--font-size-lg);
    color: var(--color-text-muted);
    max-width: 50ch;
    margin: 0 auto var(--space-xl);
}

.error-actions {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
}

@media (max-width: 768px) {
    .error-content {
        margin-top: -2rem;
    }
}
</style>
